export interface Theme {
    primary: string;
    secondary: string;
    tertiary: string;
}

export const defaultTheme: Theme = {
    primary: "#000000",
    secondary: "#FFFFFF",
    tertiary: "#000000",
};
